# SCP-173 Comma Body — Comma 4 runtime dependencies
#
# Install on the Comma 4 via SSH:
#   uv pip install -r scp173/requirements-comma4.txt
#
# numpy and opencv-python-headless are already provided by openpilot's
# pyproject.toml — listed here for completeness, pip/uv will skip them
# if already satisfied.

# Perception inference — CPU ONNX Runtime for Snapdragon 845 (aarch64)
onnxruntime >= 1.18.0

# Attention detection — MediaPipe Face Mesh (head pose + eye state)
mediapipe >= 0.10.14

# Audio — scrape loop + strike stinger
pygame >= 2.5.2

# Already in openpilot (listed for completeness)
numpy >= 2.0
opencv-python-headless >= 4.10.0
